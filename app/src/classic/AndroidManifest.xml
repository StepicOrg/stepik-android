<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="org.stepic.droid"
    xmlns:android="http://schemas.android.com/apk/res/android"
    >

    <application
        android:allowBackup="false">

        <activity
            android:name="org.stepic.droid.ui.activities.LoginActivity">

            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <link android:host="@string/host_name"/>
                <link android:host="@string/secondary_host_name"/>
                <link android:pathPattern="/oauth"/>
                <link android:scheme="stepic"/>

            </intent-filter>
        </activity>

    </application>
</manifest>
