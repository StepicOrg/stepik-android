package org.stepic.droid.ui.activities.contracts

/**
 * tag interface for activities, where should be close button at top left
 * Fragment check it (context is CloseButtonInToolbar) and set the close button
 */
interface CloseButtonInToolbar
